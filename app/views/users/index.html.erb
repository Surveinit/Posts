<h1>Users#index</h1>

<ul>
  <% if @users.any? %>
    <% @users.each do |user| %>
      <%= link_to user.email, user_path(user) %> <br>
      <p>Since: <%= user.created_at%></p> <br>
    <% end %>
  <% else %>
    <p>No Users</p>
  <% end %>
</ul>
